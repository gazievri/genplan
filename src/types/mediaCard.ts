export type MediaCardType = {
    id: number;
    title: string;
    link: string;
    description: string;
    date: string;
    image: string;
  };

export type MediaCardError = {
    message: string;
    documnetation_url: string;
  };
